ANDROID_SERIAL="${ANDROID_SERIAL:-3B15B800A0R00000}"
ADB_BIN="${ADB_BIN:-adb}"
ADB_SERVER_PORT="${ADB_SERVER_PORT:-5037}"

QWEN_CODE_ROOT="$(CDPATH='' cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PACE_CODE_ROOT="${PACE_CODE_ROOT:-${QWEN_CODE_ROOT}/..}"
QWEN_BUNDLE_ROOT="${QWEN_BUNDLE_ROOT:-${QWEN_CODE_ROOT}/../../../qwen25_3b_phone_bundle}"
QWEN_MODEL_ROOT="${QWEN_MODEL_ROOT:-${QWEN_CODE_ROOT}/../../../qwen25_3b_qnn_artifacts_hybrid_1024_balanced}"
RESULTS_ROOT="${RESULTS_ROOT:-${PACE_CODE_ROOT}/../results}"
DEVICE_LABEL="${DEVICE_LABEL:-oneplus}"
MODEL_RESULT_NAME="${MODEL_RESULT_NAME:-qwen2.5-3b}"
QUANTIZATION_RESULT_NAME="${QUANTIZATION_RESULT_NAME:-w4a16-block16}"
PERFORMANCE_MODE_RESULT_NAME="${PERFORMANCE_MODE_RESULT_NAME:-balanced}"
PHONE_DIR="${PHONE_DIR:-/data/local/tmp/lyy/executorch/qwen25_3b_qnn237_balanced}"
PYTHON_BIN="${PYTHON_BIN:-/home/lyyyy/miniconda3/envs/pace/bin/python}"
MAX_SEQ_LEN="${MAX_SEQ_LEN:-1024}"
TELEMETRY_INTERVAL_SECONDS="${TELEMETRY_INTERVAL_SECONDS:-0.5}"
QNN_RUNNER_MAX_SECONDS="${QNN_RUNNER_MAX_SECONDS:-1800}"
DEADLINES_MS="${DEADLINES_MS:-5000,10000,20000,30000}"
HTP_PERFORMANCE_MODE_NAME="${HTP_PERFORMANCE_MODE_NAME:-balanced_aot_from_pte}"
